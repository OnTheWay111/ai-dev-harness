HARNESS_DEPLOYMENT_ENV=development
# Non-secret endpoint ID from this environment's Neon hostname (ep-...).
HARNESS_POSTGRES_ENDPOINT_ID=

# Inject AI_DEV_HARNESS_DEVELOPMENT_MIGRATION_DATABASE_URL only into the
# migration/check job. Do not expose it to the Web/API process.
MIGRATION_DATABASE_URL=
